import styled from "styled-components";

const Cart = () => {
    const Wrapper = styled.section`
        height:100vh;
    `;
    return(
        <Wrapper>Cart Page</Wrapper>
    )
}

export default Cart;