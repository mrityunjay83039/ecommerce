import styled from "styled-components";

const Products = () => {

    const Wrapper = styled.section`
        height:100vh;
    `;

    return(
        <Wrapper>
            <p>Products Page</p>
        </Wrapper>
    )
}

export default Products;